---
title: "PackWise"
tagline: "AI-powered packing list app that learns what you need"
order: 2
icon: "🧳"
tags: ["AI", "Mobile"]
stack:
  [
    "React Native",
    "Expo 54",
    "TypeScript",
    "OpenAI GPT-3.5-turbo",
    "Vercel Serverless",
    "AsyncStorage",
    "Jest",
    "Maestro",
  ]
architecture: "React Native app → Vercel serverless (OpenAI GPT-3.5-turbo) → AsyncStorage (local persistence)"
metrics:
  - "12 built-in activity focus types with seed item libraries"
  - "3-tier AI fallback (API → pattern matching → generic)"
  - "6 Maestro E2E test flows"
  - "Fractional quantity math for smart per-day scaling"
keyDecisions:
  - title: "useStorage hook over a state management library"
    description: "The data model is simple enough that a custom hook backed by AsyncStorage is clearer than Redux boilerplate. One hook handles CRUD, import/export with dedup, and first-launch seeding from deterministic IDs."
  - title: "Vercel serverless over direct OpenAI calls"
    description: "Keeps the API key out of the mobile bundle entirely. The serverless function also handles GPT's tendency to wrap JSON in markdown code fences and normalizes inconsistent field names in the response."
  - title: "GitHub dark mode palette"
    description: "The color system deliberately uses GitHub's dark mode colors — familiar to developer early adopters and visually distinctive from the typical bright-white mobile app aesthetic."
links:
  betaAccess: true
---

## Description

A React Native packing list app that generates activity-aware, duration-scaled item suggestions via OpenAI GPT-3.5-turbo through a Vercel serverless proxy. Ships with 12 built-in activity types, fractional quantity math for per-day scaling, and a 3-tier AI fallback chain (API, pattern matching, generic defaults). All data persists locally via AsyncStorage with a custom useStorage hook.

## Visual Demo

import ScreenshotGallery from "../../components/ScreenshotGallery.astro";

<ScreenshotGallery
  device="phone"
  screenshots={[
    { src: "/projects/packwise/themes.png", caption: "Activity themes" },
    {
      src: "/projects/packwise/ai-suggestions.png",
      caption: "AI-powered suggestions",
    },
    { src: "/projects/packwise/categories.png", caption: "Category browser" },
    {
      src: "/projects/packwise/packing-list.png",
      caption: "Packing checklist",
    },
  ]}
/>
